# Copilot Instructions

## Project Guidelines
- User prefers Blazor and WebAssembly projects; prioritize Blazor over Razor Pages or MVC when relevant.
- Keep UI changes consistent with the project's existing Bootstrap styling unless the user explicitly requests another UI framework.
- Remind the user to run `dotnet restore` locally after changes that modify project dependencies.
- User uses Visual Studio 2026 on Windows with PowerShell; workspace path is specified.
